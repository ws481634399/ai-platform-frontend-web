# 环境变量模板（复制为 .env.development / .env.production 后按环境填写；不含真实凭据）

# 后端 API 基础地址：dev 默认 /api（经 Vite proxy 转发至 mall-gateway，避免 CORS）；
# production 留空，由部署环境注入，禁止硬编码网关地址
VITE_API_BASE_URL=

# 应用名称（用于 index.html 标题与 Layout 头部展示）
VITE_APP_TITLE=
